# ============================================================
# browsers.nix —— 浏览器用户配置（预留）
# 该放什么：Firefox / Chromium 的用户级配置（书签/扩展/策略）
# 使用方式：programs.firefox = { enable = true; ... };
# 注意：浏览器二进制由系统层 packages.nix 安装（firefox/chromium/google-chrome）
# ============================================================
{ ... }:

{
  # 预留：需要时在此添加浏览器配置
}
