# ============================================================
# gh.nix —— GitHub CLI
# ============================================================
{ pkgs, ... }:

{
  programs.gh = {
    enable = true;
    settings = {
      git_protocol = "ssh";
    };
  };
}
