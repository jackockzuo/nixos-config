# ============================================================
# gh.nix —— GitHub CLI
# ============================================================
_:

{
  programs.gh = {
    enable = true;
    settings = {
      git_protocol = "ssh";
    };
  };
}
