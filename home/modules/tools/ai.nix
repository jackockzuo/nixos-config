# ============================================================
# ai.nix —— 本地 AI（预留）
# 该放什么：Ollama / LM Studio / Chatbox 等本地模型工具
# 使用方式：home.packages = with pkgs; [ ollama ... ];
# ============================================================

{ pkgs, ... }:

{
  # 终端内的 AI 编码代理
  home.packages = with pkgs; [
    
  ];
}
