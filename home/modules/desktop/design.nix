# ============================================================
# design.nix —— 设计创作（预留）
# 该放什么：GIMP / Krita / Blender / Inkscape 等创作工具
# 使用方式：home.packages = with pkgs; [ gimp krita ... ];
# ============================================================
{ pkgs, ... }:

{
  # 预留：需要时在此添加设计工具
  home.packages = with pkgs; [ ];
}
