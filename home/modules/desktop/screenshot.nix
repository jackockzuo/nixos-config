_:

{
  # ---- 5. satty 截图标注 ----
  # 效果：默认画笔、右键直接保存到剪贴板、缩放 1.1、Noto Sans CJK SC + 中文回退字体
  xdg.configFile."satty/config.toml" = {
    source = ../../source/beautify/satty/config.toml;
    force = true; # 覆盖原作者旧配置
  };

}
