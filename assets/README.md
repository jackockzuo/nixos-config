# assets/ —— 系统级静态资产

仅在 NixOS 系统层（root / 引导器等）使用的静态文件放这里，例如引导主题、壁纸、图标。
**用户级**静态资产放 `home/source/`（判定树见 `STANDARDS.md` §3.1）。

> 2026-09：Limine 配色改由 `catppuccin.limine` 提供、壁纸不再使用，
> 原 `assets/wallpaper/` 与 `assets/catppuccin-limine/` 已删除（含 git 历史）。
