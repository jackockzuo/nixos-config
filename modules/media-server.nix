# ============================================================
# media-server.nix —— 媒体服务（预留）
# 该放什么：Jellyfin / Plex / Navidrome 等媒体服务器（需 root 常驻）
# 使用方式：services.jellyfin = { enable = true; ... }
# 注意：桌面端播放器（Spotify 等）放 home-manager/desktop/media.nix
# ============================================================
{ ... }:

{
  # 预留：需要时在此添加媒体服务器
}
