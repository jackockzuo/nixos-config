# ============================================================
# databases.nix —— 数据库服务（预留）
# 该放什么：PostgreSQL / MySQL / MariaDB / Redis 等常驻数据库服务
# 使用方式：services.postgresql = { enable = true; ... }
# 注意：临时数据库/嵌入式（sqlite）不需要常驻服务，不用放这里
# ============================================================
_:

{
  # 预留：需要时在此添加数据库服务
}
