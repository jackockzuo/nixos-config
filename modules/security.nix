# ============================================================
# security.nix —— 系统安全（预留）
# 该放什么：fail2ban / auditd / 安全加固 / 密钥管理服务
# 使用方式：services.fail2ban = { enable = true; ... }
# ============================================================
{ ... }:

{
  # 预留：需要时在此添加安全配置
}
