# ============================================================
# syncthing.nix —— 文件同步（预留）
# 该放什么：Syncthing 跨设备文件同步服务
# 使用方式：services.syncthing = { enable = true; ... }
# ============================================================
_:

{
  # 预留：需要时在此添加 Syncthing 配置
}
