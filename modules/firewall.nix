# ============================================================
# firewall.nix —— 防火墙（预留）
# 该放什么：nftables / ufw / firewalld 等防火墙规则
# 使用方式：services.nftables = { enable = true; ... }
# 注意：默认 NixOS 防火墙未开启，按需启用
# ============================================================
_:

{
  # 预留：需要时在此添加防火墙配置
}
